export enum APIS {
    appointments = "appointments",
    records = "records",
    chats = "chats"
}

export enum METHODS {
    GET = "GET",
    POST = "POST",
    PATCH = "PATCH",
    DELETE = "DELETE"
}