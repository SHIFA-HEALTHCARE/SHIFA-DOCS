export enum APIS {
    appointments = "appointments",
    records = "records",
    chats = "chats",
    users="users",
    messages="messages",
    models="models",
}
export enum PARAMETERS {
    QUERY="Query",
    BODY="Body",
    PARAMETERS="Parameters",
    HEADER="Headers"
}

export enum METHODS {
    GET = "GET",
    POST = "POST",
    PATCH = "PATCH",
    DELETE = "DELETE"
}